{
  "name": "amynest-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Render legacy entrypoint — prefer docker/backend/Dockerfile. Native build: npm ci (not npm install).",
  "main": "server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "node ./scripts/render-build.mjs",
    "dev": "pnpm --dir .. run dev:api",
    "start": "node server.js"
  },
  "dependencies": {}
}
