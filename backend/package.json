{
  "name": "amynest-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Amynest API — Render Web Service",
  "main": "server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
