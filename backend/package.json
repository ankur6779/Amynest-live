{
  "name": "amynest-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Render entrypoint — runs full @workspace/api-server from repo root",
  "main": "server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "postinstall": "node ./scripts/render-build.mjs",
    "start": "node server.js"
  },
  "dependencies": {}
}
