*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
local.properties

# Keystore — NEVER commit
*.jks
*.keystore
keystore.properties
release-signing.properties

# Firebase — google-services.json is per-Firebase-project and contains the
# project's OAuth client IDs and API keys; download a fresh copy from the
# Firebase Console (com.kidschedule.app) and drop it into app/.
# See app/PUSH_SETUP.md for full instructions.
app/google-services.json
