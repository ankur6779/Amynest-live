/**
 * Shared Experience Definition contract.
 * Packs own definitions. Resolver consumes them.
 */

export {
  EXPERIENCE_DEFINITION_VERSION,
  type ExperienceDefinition,
  type ExperienceSurfaceBinding,
  type ExperienceSurfaceBindings,
  type ExperienceSurfaceId,
} from "./types";
