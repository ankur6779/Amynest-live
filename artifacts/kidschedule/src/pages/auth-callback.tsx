import AuthActionPage from "@/pages/auth-action-page";

/**
 * Firebase email action handler at /auth/action (and legacy /auth/callback, /verify).
 */
export default function AuthCallbackPage() {
  return <AuthActionPage />;
}
