# CSS Debt Report — Phase 1

**Status:** Remaining debt after Nest Presence system manufacturing  
**Do not fix here unless Founder opens Phase 2**  
**Regression lock:** No copy · no features · no Brain · no routes · no APIs

---

## Cleared in Phase 1

| Debt | Resolution |
|------|------------|
| Dual craft facade (Nest + experience-system) | DELETE SaaS re-exports |
| Prepare shimmer SaaS | Nest `.v2-prepare-skeleton` |
| Hero optical split 36/34 | One hero 36 |
| Magic stage heights / sheet Z / fade invents | `V2_LAYOUT` · `V2_FADE_RISE_PX` |
| Prepare / sheet `text-sm` · Study prepare `text-xl` | Constitution type |

---

## Remaining Nest-adjacent debt (Phase 2 candidates)

| ID | Item | Severity | Why left |
|----|------|----------|----------|
| D1 | **Assistant black-box** (`/pages/assistant`) still chatbot chrome inside Hearing | High | Engine not Nest; room shell translated only |
| D2 | **Landing legacy branches** — non-nest path keeps `rounded-xl`, `min-h-[48px]`, kit focus | Medium | Threshold Nest path partial; full landing rewrite = redesign risk |
| D3 | **Classic `mobile-tab-bar.tsx`** uses `PRESS_FEEDBACK` / experience-system | Medium | Nest uses `V2MobileTabBar`; classic shell out of Nest flag |
| D4 | **Learning / Discovery / Math engines** still SaaS cards & radii | High | Outside Room Recovery freeze; Phase 2+ engine translation |
| D5 | **shadcn `Button` size="lg"** may fight Bloom anatomy optically | Low | Class override usually wins; audit under real device |
| D6 | **Interaction hover fills** still literal `bg-foreground/[0.10]` not CSS vars | Low | Same optical; can bind `--v2-fill-*` in Phase 2 |
| D7 | **`max-w-sm` / `max-w-md`** body/sheet widths not in `--v2-*` | Low | Stable; optional measure tokens |
| D8 | **Native store paywall sheet** after Continuity continue | Product | Not CSS; prior Continuity debt |
| D9 | **Lighting CSS** still separate from fill tokens (by design) | Info | Hour illumination; system owns materials |

---

## Duplication still outside Nest

`@/lib/experience-system` remains for classic product surfaces (phonics, discovery, screen-shell). That is intentional isolation — Nest must not re-import it.

**Rule:** Nest screens → `@/v2/craft` only.  
If a Nest file imports experience-system again → **FAIL**.

---

## Estimated scores

| Lens | Score | Notes |
|------|------:|-------|
| **Founder Nest Presence (shell)** | **72 / 100** | One system lived; engines still break the Home |
| **Apple craft (shell)** | **64 / 100** | Materials quiet; Assistant + engines + classic chrome cap ceiling |
| **Consistency (Nest rooms)** | **78 / 100** | Strong within V2; weak across whole app |
| **Public GA visual** | **NO-GO** | Aligns with Final Home ship recommendation |

---

## Founder review asks

1. Approve Nest Presence CSS system as production lock?  
2. Open Phase 2 on engines (Assistant first) or keep shell-only dogfood?  
3. Force Threshold landing full Nest-only path (delete legacy branch)?

---

## STOP

Await Founder review. **Do not start Phase 2.**
