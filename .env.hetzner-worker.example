# Hetzner AI worker overrides — copy to hetzner-overrides.env (gitignored) or export before deploy.
# Production data plane is Coolify Postgres + Coolify Redis (via host proxy or internal URL).

# Required: Coolify Redis reachable from Hetzner (example uses host proxy on Coolify)
REDIS_URL_EXTERNAL=redis://default:PASSWORD@188.245.208.126:6379

# Optional: Coolify Postgres via host proxy
# DATABASE_URL_EXTERNAL=postgresql://postgres:PASSWORD@188.245.208.126:5432/postgres

HETZNER_HOST=167.233.39.146
HETZNER_SSH_KEY=~/.ssh/id_ed25519_hetzner
