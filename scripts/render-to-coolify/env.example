# Render → Coolify PostgreSQL migration credentials
# Copy to a secure location (never commit real values).

# Render production (external URL from Render dashboard → amynest-db-dykj)
RENDER_DATABASE_URL=postgresql://USER:PASS@dpg-d85k80jtqb8s7382m7lg-a.singapore-postgres.render.com/amynest_db_jnen?sslmode=require

# Coolify Postgres — use internal Docker hostname FROM THE COOLIFY HOST (188.245.208.126):
#   postgresql://postgres:PASS@tcl9udyxcuq2zu598ebj0pfu:5432/postgres
COOLIFY_DATABASE_URL=postgresql://postgres:PASS@tcl9udyxcuq2zu598ebj0pfu:5432/postgres

# Coolify backend public URL (Traefik / sslip.io or custom domain)
COOLIFY_API_URL=https://ik6ml2uhw6op765lo14wn5m3.188.245.208.126.sslip.io

# ─── Smoke test auth (pick one path) ─────────────────────────────────────────
# Option A: paste a fresh Firebase ID token from browser DevTools
# SMOKE_FIREBASE_ID_TOKEN=eyJ...

# Option B: mint via helper (requires same Firebase env as backend)
# eval "$(bash scripts/render-to-coolify/mint-smoke-firebase-token.sh)"

# Readiness + webhook validation (copy from Coolify backend env)
# INTERNAL_HEALTH_SECRET=
# REVENUECAT_WEBHOOK_SECRET=

# Optional: direct BullMQ worker probe (run from worker host or SSH tunnel)
# SMOKE_WORKER_HEALTH_URL=http://127.0.0.1:9090/health
# SMOKE_CHILD_ID=42
# SMOKE_SKIP_AI=1

# ─── Single Active Scheduler (pre-cutover) ───────────────────────────────────
# Render: SCHEDULER_ACTIVE_PLANE=render, BACKGROUND_TASKS_ENABLED=true, NOTIFICATIONS_ENABLED=true
# Coolify: SCHEDULER_ACTIVE_PLANE=render, BACKGROUND_TASKS_ENABLED=false, NOTIFICATIONS_ENABLED=false
# bash scripts/render-to-coolify/10-scheduler-presync-render.sh

# ─── Canary monitor ──────────────────────────────────────────────────────────
# RENDER_API_URL=https://ik6ml2uhw6op765lo14wn5m3.188.245.208.126.sslip.io
# Cloudflare worker canary (wrangler.toml):
#   CANARY_BACKEND_ORIGIN=https://your-coolify-backend
#   CANARY_PERCENT=1   # stages: 1 → 10 → 25 → 50 → 100
